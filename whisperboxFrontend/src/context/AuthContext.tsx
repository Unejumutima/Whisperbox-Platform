// AuthContext — placeholder, will be fully implemented in the next phase
// Keeps the folder meaningful and lets App.tsx import it without errors

export {}   // prevents "no exports" TS error — remove once implemented
